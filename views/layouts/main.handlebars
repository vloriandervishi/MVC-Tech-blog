<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <div class="wrapper">
      <header class="hero">
          <div>
            <nav class"nav">
                    <a class="link" href="/">Home</a>
                    <a class="link"  href="/dashboard"><span id="selectedText">Dashboard</span></a>
                    {{#if loggedIn}}
                    <a class="link" href="/" id="logout">Logout</a>
                    {{else}}
                    <a class="link" href="/login">Login</a>
                    {{/if}}
                </nav>          
          </div>

          <h1 class="tech-blog-title"><i class="fab "></i><span class="glow" id="changeDashboard">Tech Blog</span></h1>
      </header>

    <main>
      {{{ body }}}
    </main>
  </div>
     
    {{#if loggedIn}}
    <script src="/Javascript/logout.js"></script>
    
    {{/if}}
   
    
</body>
